# Exceptions — run `2026-09-03T05:26:40Z-8abdcbb`  ·  `HELD_OUT`

**113 escalated** · ₹75,244.41 escalated · **13.6%** of ₹5,54,428.24 processed

113 exceptions total, sorted by rupees at risk, no truncation.

---

### `exc_00001`  ·  D06  ·  ₹3,894.80 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00132` (intent), `ord_00132` (order), `txfr_00132` (transfer) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. amount_at_risk
       amount_at_risk   over {'amount_at_risk': 389480}
       = 389480 paise
```

---

### `exc_00002`  ·  D07  ·  ₹3,682.36 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00011` (intent), `ord_00011` (order), `rvsl_00001` (reversal) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. amount_at_risk
       amount_at_risk   over {'amount_at_risk': 368236}
       = 368236 paise
```

---

### `exc_00003`  ·  UNMATCHED  ·  ₹2,037.04 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00140` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00004`  ·  D06  ·  ₹1,972.07 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00146` (intent), `ord_00146` (order), `txfr_00146` (transfer) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. amount_at_risk
       amount_at_risk   over {'amount_at_risk': 197207}
       = 197207 paise
```

---

### `exc_00005`  ·  UNMATCHED  ·  ₹1,926.18 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00127` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00006`  ·  UNMATCHED  ·  ₹1,926.18 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00128` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00007`  ·  UNMATCHED  ·  ₹1,920.22 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00162` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00008`  ·  UNMATCHED  ·  ₹1,878.30 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00115` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00009`  ·  UNMATCHED  ·  ₹1,878.29 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00114` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00010`  ·  D06  ·  ₹1,850.94 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00065` (intent), `ord_00065` (order), `txfr_00065` (transfer) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. amount_at_risk
       amount_at_risk   over {'amount_at_risk': 185094}
       = 185094 paise
```

---

### `exc_00011`  ·  UNMATCHED  ·  ₹1,845.11 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00157` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00012`  ·  UNMATCHED  ·  ₹1,766.18 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00124` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00013`  ·  UNMATCHED  ·  ₹1,582.78 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00112` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00014`  ·  UNMATCHED  ·  ₹1,576.24 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00122` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00015`  ·  UNMATCHED  ·  ₹1,546.05 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00152` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00016`  ·  UNMATCHED  ·  ₹1,504.64 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00126` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00017`  ·  UNMATCHED  ·  ₹1,504.63 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00125` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00018`  ·  D06  ·  ₹1,499.37 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00107` (intent), `ord_00107` (order), `txfr_00107` (transfer) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. amount_at_risk
       amount_at_risk   over {'amount_at_risk': 149937}
       = 149937 paise
```

---

### `exc_00019`  ·  UNMATCHED  ·  ₹1,471.42 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00160` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00020`  ·  UNMATCHED  ·  ₹1,386.24 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00143` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00021`  ·  D06  ·  ₹1,383.68 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00161` (intent), `ord_00161` (order), `txfr_00161` (transfer) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. amount_at_risk
       amount_at_risk   over {'amount_at_risk': 138368}
       = 138368 paise
```

---

### `exc_00022`  ·  D06  ·  ₹1,280.18 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00079` (intent), `ord_00079` (order), `txfr_00079` (transfer) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. amount_at_risk
       amount_at_risk   over {'amount_at_risk': 128018}
       = 128018 paise
```

---

### `exc_00023`  ·  UNMATCHED  ·  ₹1,223.55 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00132` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00024`  ·  UNMATCHED  ·  ₹1,223.54 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00131` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00025`  ·  UNMATCHED  ·  ₹1,215.52 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00120` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00026`  ·  UNMATCHED  ·  ₹1,183.64 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00117` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00027`  ·  UNMATCHED  ·  ₹1,183.64 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00118` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00028`  ·  UNMATCHED  ·  ₹1,165.30 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00134` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00029`  ·  UNMATCHED  ·  ₹1,165.29 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00133` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00030`  ·  UNMATCHED  ·  ₹1,158.14 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00164` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00031`  ·  UNMATCHED  ·  ₹1,158.14 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00165` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00032`  ·  UNMATCHED  ·  ₹1,152.64 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00121` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00033`  ·  UNMATCHED  ·  ₹1,145.84 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00146` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00034`  ·  D06  ·  ₹1,135.65 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00008` (intent), `ord_00008` (order), `txfr_00008` (transfer) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. amount_at_risk
       amount_at_risk   over {'amount_at_risk': 113565}
       = 113565 paise
```

---

### `exc_00035`  ·  D03  ·  ₹1,100.10 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00045` (intent), `ord_00045` (order), `rfnd_00009` (refund), `setl_00043` (recon_row) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. commission
       (gross_paise * commission_bps + 5000) // 10000   over {'commission_bps': 1500, 'gross_paise': 418926}
       = 62839 paise
  2. expected_transfer
       gross_paise - commission - mdr   over {'commission': 62839, 'gross_paise': 418926, 'mdr': 7541}
       = 348546 paise
  3. expected_dispute_debit
       min(dispute_total, pre_debit_transfer)   over {'dispute_total': 0, 'pre_debit_transfer': 348546}
       = 0 paise
  4. expected_refund_debit
       max(0, min(refund_total, pre_debit_transfer - expected_dispute_debit))   over {'expected_dispute_debit': 0, 'pre_debit_transfer': 348546, 'refund_total': 110010}
       = 110010 paise
  5. expected_debit
       expected_dispute_debit + expected_refund_debit   over {'expected_dispute_debit': 0, 'expected_refund_debit': 110010}
       = 110010 paise
  6. actual_debit
       actual_debit   over {'actual_debit': 0}
       = 0 paise
  7. delta
       expected_debit - actual_debit   over {'actual_debit': 0, 'expected_debit': 110010}
       = 110010 paise
```

---

### `exc_00036`  ·  D03  ·  ₹931.09 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00164` (intent), `ord_00164` (order), `rfnd_00022` (refund), `setl_00152` (recon_row) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. commission
       (gross_paise * commission_bps + 5000) // 10000   over {'commission_bps': 1500, 'gross_paise': 350033}
       = 52505 paise
  2. expected_transfer
       gross_paise - commission - mdr   over {'commission': 52505, 'gross_paise': 350033, 'mdr': 6301}
       = 291227 paise
  3. expected_dispute_debit
       min(dispute_total, pre_debit_transfer)   over {'dispute_total': 0, 'pre_debit_transfer': 291227}
       = 0 paise
  4. expected_refund_debit
       max(0, min(refund_total, pre_debit_transfer - expected_dispute_debit))   over {'expected_dispute_debit': 0, 'pre_debit_transfer': 291227, 'refund_total': 93109}
       = 93109 paise
  5. expected_debit
       expected_dispute_debit + expected_refund_debit   over {'expected_dispute_debit': 0, 'expected_refund_debit': 93109}
       = 93109 paise
  6. actual_debit
       actual_debit   over {'actual_debit': 0}
       = 0 paise
  7. delta
       expected_debit - actual_debit   over {'actual_debit': 0, 'expected_debit': 93109}
       = 93109 paise
```

---

### `exc_00037`  ·  D03  ·  ₹900.31 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00036` (intent), `ord_00036` (order), `rfnd_00008` (refund), `setl_00034` (recon_row) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. commission
       (gross_paise * commission_bps + 5000) // 10000   over {'commission_bps': 1600, 'gross_paise': 244516}
       = 39123 paise
  2. expected_transfer
       gross_paise - commission - mdr   over {'commission': 39123, 'gross_paise': 244516, 'mdr': 4401}
       = 200992 paise
  3. expected_dispute_debit
       min(dispute_total, pre_debit_transfer)   over {'dispute_total': 0, 'pre_debit_transfer': 200992}
       = 0 paise
  4. expected_refund_debit
       max(0, min(refund_total, pre_debit_transfer - expected_dispute_debit))   over {'expected_dispute_debit': 0, 'pre_debit_transfer': 200992, 'refund_total': 90031}
       = 90031 paise
  5. expected_debit
       expected_dispute_debit + expected_refund_debit   over {'expected_dispute_debit': 0, 'expected_refund_debit': 90031}
       = 90031 paise
  6. actual_debit
       actual_debit   over {'actual_debit': 0}
       = 0 paise
  7. delta
       expected_debit - actual_debit   over {'actual_debit': 0, 'expected_debit': 90031}
       = 90031 paise
```

---

### `exc_00038`  ·  D03  ·  ₹844.26 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00116` (intent), `ord_00116` (order), `rfnd_00017` (refund), `setl_00107` (recon_row) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. commission
       (gross_paise * commission_bps + 5000) // 10000   over {'commission_bps': 1500, 'gross_paise': 466184}
       = 69928 paise
  2. expected_transfer
       gross_paise - commission - mdr   over {'commission': 69928, 'gross_paise': 466184, 'mdr': 6993}
       = 389263 paise
  3. expected_dispute_debit
       min(dispute_total, pre_debit_transfer)   over {'dispute_total': 0, 'pre_debit_transfer': 389263}
       = 0 paise
  4. expected_refund_debit
       max(0, min(refund_total, pre_debit_transfer - expected_dispute_debit))   over {'expected_dispute_debit': 0, 'pre_debit_transfer': 389263, 'refund_total': 84426}
       = 84426 paise
  5. expected_debit
       expected_dispute_debit + expected_refund_debit   over {'expected_dispute_debit': 0, 'expected_refund_debit': 84426}
       = 84426 paise
  6. actual_debit
       actual_debit   over {'actual_debit': 0}
       = 0 paise
  7. delta
       expected_debit - actual_debit   over {'actual_debit': 0, 'expected_debit': 84426}
       = 84426 paise
```

---

### `exc_00039`  ·  D07  ·  ₹804.80 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00082` (intent), `ord_00082` (order), `rvsl_00003` (reversal) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. amount_at_risk
       amount_at_risk   over {'amount_at_risk': 80480}
       = 80480 paise
```

---

### `exc_00040`  ·  UNMATCHED  ·  ₹764.65 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00109` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00041`  ·  UNMATCHED  ·  ₹721.32 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00138` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00042`  ·  UNMATCHED  ·  ₹708.34 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00154` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00043`  ·  UNMATCHED  ·  ₹673.91 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00158` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00044`  ·  UNMATCHED  ·  ₹659.72 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00119` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00045`  ·  D03  ·  ₹647.28 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00030` (intent), `ord_00030` (order), `rfnd_00006` (refund), `setl_00028` (recon_row) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. commission
       (gross_paise * commission_bps + 5000) // 10000   over {'commission_bps': 1600, 'gross_paise': 250787}
       = 40126 paise
  2. expected_transfer
       gross_paise - commission - mdr   over {'commission': 40126, 'gross_paise': 250787, 'mdr': 2508}
       = 208153 paise
  3. expected_dispute_debit
       min(dispute_total, pre_debit_transfer)   over {'dispute_total': 0, 'pre_debit_transfer': 208153}
       = 0 paise
  4. expected_refund_debit
       max(0, min(refund_total, pre_debit_transfer - expected_dispute_debit))   over {'expected_dispute_debit': 0, 'pre_debit_transfer': 208153, 'refund_total': 64728}
       = 64728 paise
  5. expected_debit
       expected_dispute_debit + expected_refund_debit   over {'expected_dispute_debit': 0, 'expected_refund_debit': 64728}
       = 64728 paise
  6. actual_debit
       actual_debit   over {'actual_debit': 0}
       = 0 paise
  7. delta
       expected_debit - actual_debit   over {'actual_debit': 0, 'expected_debit': 64728}
       = 64728 paise
```

---

### `exc_00046`  ·  UNMATCHED  ·  ₹622.83 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00144` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00047`  ·  UNMATCHED  ·  ₹622.83 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00145` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00048`  ·  UNMATCHED  ·  ₹534.85 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00116` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00049`  ·  UNMATCHED  ·  ₹532.11 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00104` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00050`  ·  D03  ·  ₹503.13 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00014` (intent), `ord_00014` (order), `rfnd_00002` (refund), `setl_00013` (recon_row) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. commission
       (gross_paise * commission_bps + 5000) // 10000   over {'commission_bps': 1500, 'gross_paise': 179624}
       = 26944 paise
  2. expected_transfer
       gross_paise - commission - mdr   over {'commission': 26944, 'gross_paise': 179624, 'mdr': 3233}
       = 149447 paise
  3. expected_dispute_debit
       min(dispute_total, pre_debit_transfer)   over {'dispute_total': 0, 'pre_debit_transfer': 149447}
       = 0 paise
  4. expected_refund_debit
       max(0, min(refund_total, pre_debit_transfer - expected_dispute_debit))   over {'expected_dispute_debit': 0, 'pre_debit_transfer': 149447, 'refund_total': 50313}
       = 50313 paise
  5. expected_debit
       expected_dispute_debit + expected_refund_debit   over {'expected_dispute_debit': 0, 'expected_refund_debit': 50313}
       = 50313 paise
  6. actual_debit
       actual_debit   over {'actual_debit': 0}
       = 0 paise
  7. delta
       expected_debit - actual_debit   over {'actual_debit': 0, 'expected_debit': 50313}
       = 50313 paise
```

---

### `exc_00051`  ·  UNMATCHED  ·  ₹472.39 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00113` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00052`  ·  D01  ·  ₹471.64 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00046` (intent), `ord_00046` (order), `rate_00004` (rate_card) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. contracted_commission
       (gross_paise * contracted_bps + 5000) // 10000   over {'contracted_bps': 1800, 'gross_paise': 278419}
       = 50115 paise
  2. applied_commission
       (gross_paise * applied_bps + 5000) // 10000   over {'applied_bps': 106, 'gross_paise': 278419}
       = 2951 paise
  3. delta
       abs(applied_commission - contracted_commission)   over {'applied_commission': 2951, 'contracted_commission': 50115}
       = 47164 paise
```

---

### `exc_00053`  ·  UNMATCHED  ·  ₹430.70 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00101` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00054`  ·  UNMATCHED  ·  ₹424.47 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00163` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00055`  ·  D07  ·  ₹417.97 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00120` (intent), `ord_00120` (order), `rvsl_00004` (reversal) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. amount_at_risk
       amount_at_risk   over {'amount_at_risk': 41797}
       = 41797 paise
```

---

### `exc_00056`  ·  UNMATCHED  ·  ₹402.40 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00150` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00057`  ·  UNMATCHED  ·  ₹402.40 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00151` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00058`  ·  D01  ·  ₹393.59 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00193` (intent), `ord_00193` (order), `rate_00003` (rate_card) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. contracted_commission
       (gross_paise * contracted_bps + 5000) // 10000   over {'contracted_bps': 800, 'gross_paise': 119305}
       = 9544 paise
  2. applied_commission
       (gross_paise * applied_bps + 5000) // 10000   over {'applied_bps': 4099, 'gross_paise': 119305}
       = 48903 paise
  3. delta
       abs(applied_commission - contracted_commission)   over {'applied_commission': 48903, 'contracted_commission': 9544}
       = 39359 paise
```

---

### `exc_00059`  ·  UNMATCHED  ·  ₹387.89 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00156` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00060`  ·  UNMATCHED  ·  ₹387.88 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00155` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00061`  ·  UNMATCHED  ·  ₹361.71 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00103` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00062`  ·  UNMATCHED  ·  ₹361.70 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00102` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00063`  ·  UNMATCHED  ·  ₹346.54 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00110` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00064`  ·  UNMATCHED  ·  ₹346.54 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00111` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00065`  ·  UNMATCHED  ·  ₹321.60 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00135` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00066`  ·  UNMATCHED  ·  ₹313.50 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00149` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00067`  ·  UNMATCHED  ·  ₹306.81 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00123` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00068`  ·  UNMATCHED  ·  ₹301.85 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00107` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00069`  ·  UNMATCHED  ·  ₹301.85 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00108` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00070`  ·  UNMATCHED  ·  ₹289.87 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00153` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00071`  ·  D03  ·  ₹178.26 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00017` (intent), `ord_00017` (order), `rfnd_00003` (refund), `setl_00016` (recon_row) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. commission
       (gross_paise * commission_bps + 5000) // 10000   over {'commission_bps': 1500, 'gross_paise': 92942}
       = 13941 paise
  2. expected_transfer
       gross_paise - commission - mdr   over {'commission': 13941, 'gross_paise': 92942, 'mdr': 1673}
       = 77328 paise
  3. expected_dispute_debit
       min(dispute_total, pre_debit_transfer)   over {'dispute_total': 0, 'pre_debit_transfer': 77328}
       = 0 paise
  4. expected_refund_debit
       max(0, min(refund_total, pre_debit_transfer - expected_dispute_debit))   over {'expected_dispute_debit': 0, 'pre_debit_transfer': 77328, 'refund_total': 17826}
       = 17826 paise
  5. expected_debit
       expected_dispute_debit + expected_refund_debit   over {'expected_dispute_debit': 0, 'expected_refund_debit': 17826}
       = 17826 paise
  6. actual_debit
       actual_debit   over {'actual_debit': 0}
       = 0 paise
  7. delta
       expected_debit - actual_debit   over {'actual_debit': 0, 'expected_debit': 17826}
       = 17826 paise
```

---

### `exc_00072`  ·  D01  ·  ₹152.23 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00037` (intent), `ord_00037` (order), `rate_00002` (rate_card) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. contracted_commission
       (gross_paise * contracted_bps + 5000) // 10000   over {'contracted_bps': 1500, 'gross_paise': 397481}
       = 59622 paise
  2. applied_commission
       (gross_paise * applied_bps + 5000) // 10000   over {'applied_bps': 1117, 'gross_paise': 397481}
       = 44399 paise
  3. delta
       abs(applied_commission - contracted_commission)   over {'applied_commission': 44399, 'contracted_commission': 59622}
       = 15223 paise
```

---

### `exc_00073`  ·  UNMATCHED  ·  ₹143.03 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00159` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00074`  ·  D03  ·  ₹113.33 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00188` (intent), `ord_00188` (order), `rfnd_00025` (refund), `setl_00175` (recon_row) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. commission
       (gross_paise * commission_bps + 5000) // 10000   over {'commission_bps': 1500, 'gross_paise': 100466}
       = 15070 paise
  2. expected_transfer
       gross_paise - commission - mdr   over {'commission': 15070, 'gross_paise': 100466, 'mdr': 1005}
       = 84391 paise
  3. expected_dispute_debit
       min(dispute_total, pre_debit_transfer)   over {'dispute_total': 0, 'pre_debit_transfer': 84391}
       = 0 paise
  4. expected_refund_debit
       max(0, min(refund_total, pre_debit_transfer - expected_dispute_debit))   over {'expected_dispute_debit': 0, 'pre_debit_transfer': 84391, 'refund_total': 11333}
       = 11333 paise
  5. expected_debit
       expected_dispute_debit + expected_refund_debit   over {'expected_dispute_debit': 0, 'expected_refund_debit': 11333}
       = 11333 paise
  6. actual_debit
       actual_debit   over {'actual_debit': 0}
       = 0 paise
  7. delta
       expected_debit - actual_debit   over {'actual_debit': 0, 'expected_debit': 11333}
       = 11333 paise
```

---

### `exc_00075`  ·  D03  ·  ₹104.89 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00022` (intent), `ord_00022` (order), `rfnd_00004` (refund), `setl_00021` (recon_row) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. commission
       (gross_paise * commission_bps + 5000) // 10000   over {'commission_bps': 1800, 'gross_paise': 73863}
       = 13295 paise
  2. expected_transfer
       gross_paise - commission - mdr   over {'commission': 13295, 'gross_paise': 73863, 'mdr': 1330}
       = 59238 paise
  3. expected_dispute_debit
       min(dispute_total, pre_debit_transfer)   over {'dispute_total': 0, 'pre_debit_transfer': 59238}
       = 0 paise
  4. expected_refund_debit
       max(0, min(refund_total, pre_debit_transfer - expected_dispute_debit))   over {'expected_dispute_debit': 0, 'pre_debit_transfer': 59238, 'refund_total': 10489}
       = 10489 paise
  5. expected_debit
       expected_dispute_debit + expected_refund_debit   over {'expected_dispute_debit': 0, 'expected_refund_debit': 10489}
       = 10489 paise
  6. actual_debit
       actual_debit   over {'actual_debit': 0}
       = 0 paise
  7. delta
       expected_debit - actual_debit   over {'actual_debit': 0, 'expected_debit': 10489}
       = 10489 paise
```

---

### `exc_00076`  ·  D01  ·  ₹54.30 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00003` (intent), `ord_00003` (order), `rate_00002` (rate_card) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. contracted_commission
       (gross_paise * contracted_bps + 5000) // 10000   over {'contracted_bps': 1500, 'gross_paise': 445085}
       = 66763 paise
  2. applied_commission
       (gross_paise * applied_bps + 5000) // 10000   over {'applied_bps': 1378, 'gross_paise': 445085}
       = 61333 paise
  3. delta
       abs(applied_commission - contracted_commission)   over {'applied_commission': 61333, 'contracted_commission': 66763}
       = 5430 paise
```

---

### `exc_00077`  ·  D01  ·  ₹12.70 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00176` (intent), `ord_00176` (order), `rate_00008` (rate_card) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. contracted_commission
       (gross_paise * contracted_bps + 5000) // 10000   over {'contracted_bps': 1600, 'gross_paise': 144313}
       = 23090 paise
  2. applied_commission
       (gross_paise * applied_bps + 5000) // 10000   over {'applied_bps': 1512, 'gross_paise': 144313}
       = 21820 paise
  3. delta
       abs(applied_commission - contracted_commission)   over {'applied_commission': 21820, 'contracted_commission': 23090}
       = 1270 paise
```

---

### `exc_00078`  ·  D04  ·  ₹5.38 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00196` (intent), `ord_00196` (order), `rfnd_00027` (refund) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. net_of_returns
       gross_paise - refund_total   over {'gross_paise': 415180, 'refund_total': 107573}
       = 307607 paise
  2. expected_tcs
       (net_of_returns * rate_bps + 5000) // 10000   over {'net_of_returns': 307607, 'rate_bps': 50}
       = 1538 paise
  3. delta
       abs(applied_tcs - expected_tcs)   over {'applied_tcs': 2076, 'expected_tcs': 1538}
       = 538 paise
```

---

### `exc_00079`  ·  D08  ·  ₹4.37 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00062` (intent), `ord_00062` (order), `pay_00062` (payment) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. expected_gst_on_fee
       (fee_total_paise * rate_bps + 5000) // 10000   over {'fee_total_paise': 7270, 'rate_bps': 1800}
       = 1309 paise
  2. delta
       abs(applied_total - expected_total)   over {'applied_total': 872, 'expected_total': 1309}
       = 437 paise
```

---

### `exc_00080`  ·  D04  ·  ₹4.12 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00086` (intent), `ord_00086` (order), `rfnd_00014` (refund) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. net_of_returns
       gross_paise - refund_total   over {'gross_paise': 326014, 'refund_total': 82416}
       = 243598 paise
  2. expected_tcs
       (net_of_returns * rate_bps + 5000) // 10000   over {'net_of_returns': 243598, 'rate_bps': 50}
       = 1218 paise
  3. delta
       abs(applied_tcs - expected_tcs)   over {'applied_tcs': 1630, 'expected_tcs': 1218}
       = 412 paise
```

---

### `exc_00081`  ·  D02  ·  ₹3.80 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00110` (intent), `ord_00110` (order), `setl_00101` (recon_row) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. commission
       (gross_paise * commission_bps + 5000) // 10000   over {'commission_bps': 1500, 'gross_paise': 459533}
       = 68930 paise
  2. expected_transfer
       gross_paise - commission - mdr   over {'commission': 68930, 'gross_paise': 459533, 'mdr': 6893}
       = 383710 paise
  3. debit
       min(refund_total + dispute_total, expected_transfer)   over {'dispute_total': 0, 'expected_transfer': 383710, 'refund_total': 0}
       = 0 paise
  4. expected_net
       expected_transfer - debit   over {'debit': 0, 'expected_transfer': 383710}
       = 383710 paise
  5. actual_net
       total_credit_paise - total_debit_paise   over {'total_credit_paise': 383330, 'total_debit_paise': 0}
       = 383330 paise
  6. delta
       expected_net - actual_net   over {'actual_net': 383330, 'expected_net': 383710}
       = 380 paise
```

---

### `exc_00082`  ·  D04  ·  ₹2.96 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00173` (intent), `ord_00173` (order), `rfnd_00023` (refund) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. net_of_returns
       gross_paise - refund_total   over {'gross_paise': 259154, 'refund_total': 59217}
       = 199937 paise
  2. expected_tcs
       (net_of_returns * rate_bps + 5000) // 10000   over {'net_of_returns': 199937, 'rate_bps': 50}
       = 1000 paise
  3. delta
       abs(applied_tcs - expected_tcs)   over {'applied_tcs': 1296, 'expected_tcs': 1000}
       = 296 paise
```

---

### `exc_00083`  ·  D08  ·  ₹2.95 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00013` (intent), `ord_00013` (order), `pay_00013` (payment) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. expected_gst_on_fee
       (fee_total_paise * rate_bps + 5000) // 10000   over {'fee_total_paise': 4912, 'rate_bps': 1800}
       = 884 paise
  2. delta
       abs(applied_total - expected_total)   over {'applied_total': 589, 'expected_total': 884}
       = 295 paise
```

---

### `exc_00084`  ·  D02  ·  ₹2.32 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00150` (intent), `ord_00150` (order), `setl_00139` (recon_row) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. commission
       (gross_paise * commission_bps + 5000) // 10000   over {'commission_bps': 1200, 'gross_paise': 442098}
       = 53052 paise
  2. expected_transfer
       gross_paise - commission - mdr   over {'commission': 53052, 'gross_paise': 442098, 'mdr': 7958}
       = 381088 paise
  3. debit
       min(refund_total + dispute_total, expected_transfer)   over {'dispute_total': 0, 'expected_transfer': 381088, 'refund_total': 0}
       = 0 paise
  4. expected_net
       expected_transfer - debit   over {'debit': 0, 'expected_transfer': 381088}
       = 381088 paise
  5. actual_net
       total_credit_paise - total_debit_paise   over {'total_credit_paise': 380856, 'total_debit_paise': 0}
       = 380856 paise
  6. delta
       expected_net - actual_net   over {'actual_net': 380856, 'expected_net': 381088}
       = 232 paise
```

---

### `exc_00085`  ·  D02  ·  ₹1.84 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00160` (intent), `ord_00160` (order), `setl_00149` (recon_row) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. commission
       (gross_paise * commission_bps + 5000) // 10000   over {'commission_bps': 1500, 'gross_paise': 332946}
       = 49942 paise
  2. expected_transfer
       gross_paise - commission - mdr   over {'commission': 49942, 'gross_paise': 332946, 'mdr': 3329}
       = 279675 paise
  3. debit
       min(refund_total + dispute_total, expected_transfer)   over {'dispute_total': 0, 'expected_transfer': 279675, 'refund_total': 0}
       = 0 paise
  4. expected_net
       expected_transfer - debit   over {'debit': 0, 'expected_transfer': 279675}
       = 279675 paise
  5. actual_net
       total_credit_paise - total_debit_paise   over {'total_credit_paise': 279491, 'total_debit_paise': 0}
       = 279491 paise
  6. delta
       expected_net - actual_net   over {'actual_net': 279491, 'expected_net': 279675}
       = 184 paise
```

---

### `exc_00086`  ·  D08  ·  ₹1.82 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00166` (intent), `ord_00166` (order), `pay_00166` (payment) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. expected_gst_on_fee
       (fee_total_paise * rate_bps + 5000) // 10000   over {'fee_total_paise': 3028, 'rate_bps': 1800}
       = 545 paise
  2. delta
       abs(applied_total - expected_total)   over {'applied_total': 363, 'expected_total': 545}
       = 182 paise
```

---

### `exc_00087`  ·  D02  ·  ₹1.79 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00080` (intent), `ord_00080` (order), `setl_00074` (recon_row) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. commission
       (gross_paise * commission_bps + 5000) // 10000   over {'commission_bps': 1500, 'gross_paise': 260902}
       = 39135 paise
  2. expected_transfer
       gross_paise - commission - mdr   over {'commission': 39135, 'gross_paise': 260902, 'mdr': 0}
       = 221767 paise
  3. debit
       min(refund_total + dispute_total, expected_transfer)   over {'dispute_total': 0, 'expected_transfer': 221767, 'refund_total': 0}
       = 0 paise
  4. expected_net
       expected_transfer - debit   over {'debit': 0, 'expected_transfer': 221767}
       = 221767 paise
  5. actual_net
       total_credit_paise - total_debit_paise   over {'total_credit_paise': 221588, 'total_debit_paise': 0}
       = 221588 paise
  6. delta
       expected_net - actual_net   over {'actual_net': 221588, 'expected_net': 221767}
       = 179 paise
```

---

### `exc_00088`  ·  D02  ·  ₹1.63 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00136` (intent), `ord_00136` (order), `setl_00126` (recon_row) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. commission
       (gross_paise * commission_bps + 5000) // 10000   over {'commission_bps': 1800, 'gross_paise': 229657}
       = 41338 paise
  2. expected_transfer
       gross_paise - commission - mdr   over {'commission': 41338, 'gross_paise': 229657, 'mdr': 0}
       = 188319 paise
  3. debit
       min(refund_total + dispute_total, expected_transfer)   over {'dispute_total': 0, 'expected_transfer': 188319, 'refund_total': 0}
       = 0 paise
  4. expected_net
       expected_transfer - debit   over {'debit': 0, 'expected_transfer': 188319}
       = 188319 paise
  5. actual_net
       total_credit_paise - total_debit_paise   over {'total_credit_paise': 188156, 'total_debit_paise': 0}
       = 188156 paise
  6. delta
       expected_net - actual_net   over {'actual_net': 188156, 'expected_net': 188319}
       = 163 paise
```

---

### `exc_00089`  ·  D02  ·  ₹1.39 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00112` (intent), `ord_00112` (order), `setl_00103` (recon_row) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. commission
       (gross_paise * commission_bps + 5000) // 10000   over {'commission_bps': 800, 'gross_paise': 167013}
       = 13361 paise
  2. expected_transfer
       gross_paise - commission - mdr   over {'commission': 13361, 'gross_paise': 167013, 'mdr': 0}
       = 153652 paise
  3. debit
       min(refund_total + dispute_total, expected_transfer)   over {'dispute_total': 0, 'expected_transfer': 153652, 'refund_total': 0}
       = 0 paise
  4. expected_net
       expected_transfer - debit   over {'debit': 0, 'expected_transfer': 153652}
       = 153652 paise
  5. actual_net
       total_credit_paise - total_debit_paise   over {'total_credit_paise': 153513, 'total_debit_paise': 0}
       = 153513 paise
  6. delta
       expected_net - actual_net   over {'actual_net': 153513, 'expected_net': 153652}
       = 139 paise
```

---

### `exc_00090`  ·  D08  ·  ₹1.29 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00175` (intent), `ord_00175` (order), `pay_00175` (payment) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. expected_gst_on_fee
       (fee_total_paise * rate_bps + 5000) // 10000   over {'fee_total_paise': 2155, 'rate_bps': 1800}
       = 388 paise
  2. delta
       abs(applied_total - expected_total)   over {'applied_total': 259, 'expected_total': 388}
       = 129 paise
```

---

### `exc_00091`  ·  D04  ·  ₹1.27 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00031` (intent), `ord_00031` (order), `rfnd_00007` (refund) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. net_of_returns
       gross_paise - refund_total   over {'gross_paise': 222262, 'refund_total': 25493}
       = 196769 paise
  2. expected_tcs
       (net_of_returns * rate_bps + 5000) // 10000   over {'net_of_returns': 196769, 'rate_bps': 50}
       = 984 paise
  3. delta
       abs(applied_tcs - expected_tcs)   over {'applied_tcs': 1111, 'expected_tcs': 984}
       = 127 paise
```

---

### `exc_00092`  ·  D08  ·  ₹1.13 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00010` (intent), `ord_00010` (order), `pay_00010` (payment) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. expected_gst_on_fee
       (fee_total_paise * rate_bps + 5000) // 10000   over {'fee_total_paise': 1870, 'rate_bps': 1800}
       = 337 paise
  2. delta
       abs(applied_total - expected_total)   over {'applied_total': 224, 'expected_total': 337}
       = 113 paise
```

---

### `exc_00093`  ·  D02  ·  ₹0.94 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00134` (intent), `ord_00134` (order), `setl_00124` (recon_row) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. commission
       (gross_paise * commission_bps + 5000) // 10000   over {'commission_bps': 1200, 'gross_paise': 310766}
       = 37292 paise
  2. expected_transfer
       gross_paise - commission - mdr   over {'commission': 37292, 'gross_paise': 310766, 'mdr': 5594}
       = 267880 paise
  3. debit
       min(refund_total + dispute_total, expected_transfer)   over {'dispute_total': 0, 'expected_transfer': 267880, 'refund_total': 0}
       = 0 paise
  4. expected_net
       expected_transfer - debit   over {'debit': 0, 'expected_transfer': 267880}
       = 267880 paise
  5. actual_net
       total_credit_paise - total_debit_paise   over {'total_credit_paise': 267786, 'total_debit_paise': 0}
       = 267786 paise
  6. delta
       expected_net - actual_net   over {'actual_net': 267786, 'expected_net': 267880}
       = 94 paise
```

---

### `exc_00094`  ·  D02  ·  ₹0.89 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00061` (intent), `ord_00061` (order), `setl_00058` (recon_row) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. commission
       (gross_paise * commission_bps + 5000) // 10000   over {'commission_bps': 1500, 'gross_paise': 239526}
       = 35929 paise
  2. expected_transfer
       gross_paise - commission - mdr   over {'commission': 35929, 'gross_paise': 239526, 'mdr': 3593}
       = 200004 paise
  3. debit
       min(refund_total + dispute_total, expected_transfer)   over {'dispute_total': 0, 'expected_transfer': 200004, 'refund_total': 0}
       = 0 paise
  4. expected_net
       expected_transfer - debit   over {'debit': 0, 'expected_transfer': 200004}
       = 200004 paise
  5. actual_net
       total_credit_paise - total_debit_paise   over {'total_credit_paise': 199915, 'total_debit_paise': 0}
       = 199915 paise
  6. delta
       expected_net - actual_net   over {'actual_net': 199915, 'expected_net': 200004}
       = 89 paise
```

---

### `exc_00095`  ·  D05  ·  ₹0.84 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00006` (intent), `ord_00006` (order) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. expected_tds
       (gross_paise * rate_bps + 5000) // 10000   over {'gross_paise': 473152, 'rate_bps': 10}
       = 473 paise
  2. delta
       abs(applied_tds - expected_tds)   over {'applied_tds': 389, 'expected_tds': 473}
       = 84 paise
```

---

### `exc_00096`  ·  D05  ·  ₹0.77 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00058` (intent), `ord_00058` (order) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. expected_tds
       (gross_paise * rate_bps + 5000) // 10000   over {'gross_paise': 481143, 'rate_bps': 10}
       = 481 paise
  2. delta
       abs(applied_tds - expected_tds)   over {'applied_tds': 404, 'expected_tds': 481}
       = 77 paise
```

---

### `exc_00097`  ·  D05  ·  ₹0.65 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00048` (intent), `ord_00048` (order) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. expected_tds
       (gross_paise * rate_bps + 5000) // 10000   over {'gross_paise': 387990, 'rate_bps': 10}
       = 388 paise
  2. delta
       abs(applied_tds - expected_tds)   over {'applied_tds': 323, 'expected_tds': 388}
       = 65 paise
```

---

### `exc_00098`  ·  D05  ·  ₹0.63 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00043` (intent), `ord_00043` (order) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. expected_tds
       (gross_paise * rate_bps + 5000) // 10000   over {'gross_paise': 381964, 'rate_bps': 10}
       = 382 paise
  2. delta
       abs(applied_tds - expected_tds)   over {'applied_tds': 319, 'expected_tds': 382}
       = 63 paise
```

---

### `exc_00099`  ·  D02  ·  ₹0.61 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00053` (intent), `ord_00053` (order), `setl_00051` (recon_row) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. commission
       (gross_paise * commission_bps + 5000) // 10000   over {'commission_bps': 1800, 'gross_paise': 252462}
       = 45443 paise
  2. expected_transfer
       gross_paise - commission - mdr   over {'commission': 45443, 'gross_paise': 252462, 'mdr': 0}
       = 207019 paise
  3. debit
       min(refund_total + dispute_total, expected_transfer)   over {'dispute_total': 0, 'expected_transfer': 207019, 'refund_total': 0}
       = 0 paise
  4. expected_net
       expected_transfer - debit   over {'debit': 0, 'expected_transfer': 207019}
       = 207019 paise
  5. actual_net
       total_credit_paise - total_debit_paise   over {'total_credit_paise': 206958, 'total_debit_paise': 0}
       = 206958 paise
  6. delta
       expected_net - actual_net   over {'actual_net': 206958, 'expected_net': 207019}
       = 61 paise
```

---

### `exc_00100`  ·  D05  ·  ₹0.58 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00133` (intent), `ord_00133` (order) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. expected_tds
       (gross_paise * rate_bps + 5000) // 10000   over {'gross_paise': 290647, 'rate_bps': 10}
       = 291 paise
  2. delta
       abs(applied_tds - expected_tds)   over {'applied_tds': 233, 'expected_tds': 291}
       = 58 paise
```

---

### `exc_00101`  ·  D05  ·  ₹0.56 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00016` (intent), `ord_00016` (order) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. expected_tds
       (gross_paise * rate_bps + 5000) // 10000   over {'gross_paise': 406065, 'rate_bps': 10}
       = 406 paise
  2. delta
       abs(applied_tds - expected_tds)   over {'applied_tds': 350, 'expected_tds': 406}
       = 56 paise
```

---

### `exc_00102`  ·  D05  ·  ₹0.52 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00005` (intent), `ord_00005` (order) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. expected_tds
       (gross_paise * rate_bps + 5000) // 10000   over {'gross_paise': 388428, 'rate_bps': 10}
       = 388 paise
  2. delta
       abs(applied_tds - expected_tds)   over {'applied_tds': 336, 'expected_tds': 388}
       = 52 paise
```

---

### `exc_00103`  ·  D02  ·  ₹0.50 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00183` (intent), `ord_00183` (order), `setl_00170` (recon_row) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. commission
       (gross_paise * commission_bps + 5000) // 10000   over {'commission_bps': 1500, 'gross_paise': 197909}
       = 29686 paise
  2. expected_transfer
       gross_paise - commission - mdr   over {'commission': 29686, 'gross_paise': 197909, 'mdr': 3562}
       = 164661 paise
  3. debit
       min(refund_total + dispute_total, expected_transfer)   over {'dispute_total': 0, 'expected_transfer': 164661, 'refund_total': 0}
       = 0 paise
  4. expected_net
       expected_transfer - debit   over {'debit': 0, 'expected_transfer': 164661}
       = 164661 paise
  5. actual_net
       total_credit_paise - total_debit_paise   over {'total_credit_paise': 164611, 'total_debit_paise': 0}
       = 164611 paise
  6. delta
       expected_net - actual_net   over {'actual_net': 164611, 'expected_net': 164661}
       = 50 paise
```

---

### `exc_00104`  ·  D08  ·  ₹0.44 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00039` (intent), `ord_00039` (order), `pay_00039` (payment) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. expected_gst_on_fee
       (fee_total_paise * rate_bps + 5000) // 10000   over {'fee_total_paise': 727, 'rate_bps': 1800}
       = 131 paise
  2. delta
       abs(applied_total - expected_total)   over {'applied_total': 87, 'expected_total': 131}
       = 44 paise
```

---

### `exc_00105`  ·  D02  ·  ₹0.32 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00181` (intent), `ord_00181` (order), `setl_00168` (recon_row) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. commission
       (gross_paise * commission_bps + 5000) // 10000   over {'commission_bps': 1800, 'gross_paise': 55534}
       = 9996 paise
  2. expected_transfer
       gross_paise - commission - mdr   over {'commission': 9996, 'gross_paise': 55534, 'mdr': 0}
       = 45538 paise
  3. debit
       min(refund_total + dispute_total, expected_transfer)   over {'dispute_total': 0, 'expected_transfer': 45538, 'refund_total': 0}
       = 0 paise
  4. expected_net
       expected_transfer - debit   over {'debit': 0, 'expected_transfer': 45538}
       = 45538 paise
  5. actual_net
       total_credit_paise - total_debit_paise   over {'total_credit_paise': 45506, 'total_debit_paise': 0}
       = 45506 paise
  6. delta
       expected_net - actual_net   over {'actual_net': 45506, 'expected_net': 45538}
       = 32 paise
```

---

### `exc_00106`  ·  D05  ·  ₹0.27 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00135` (intent), `ord_00135` (order) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. expected_tds
       (gross_paise * rate_bps + 5000) // 10000   over {'gross_paise': 225968, 'rate_bps': 10}
       = 226 paise
  2. delta
       abs(applied_tds - expected_tds)   over {'applied_tds': 199, 'expected_tds': 226}
       = 27 paise
```

---

### `exc_00107`  ·  D02  ·  ₹0.21 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00102` (intent), `ord_00102` (order), `setl_00095` (recon_row) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. commission
       (gross_paise * commission_bps + 5000) // 10000   over {'commission_bps': 1600, 'gross_paise': 51296}
       = 8207 paise
  2. expected_transfer
       gross_paise - commission - mdr   over {'commission': 8207, 'gross_paise': 51296, 'mdr': 769}
       = 42320 paise
  3. debit
       min(refund_total + dispute_total, expected_transfer)   over {'dispute_total': 0, 'expected_transfer': 42320, 'refund_total': 0}
       = 0 paise
  4. expected_net
       expected_transfer - debit   over {'debit': 0, 'expected_transfer': 42320}
       = 42320 paise
  5. actual_net
       total_credit_paise - total_debit_paise   over {'total_credit_paise': 42299, 'total_debit_paise': 0}
       = 42299 paise
  6. delta
       expected_net - actual_net   over {'actual_net': 42299, 'expected_net': 42320}
       = 21 paise
```

---

### `exc_00108`  ·  D05  ·  ₹0.13 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00004` (intent), `ord_00004` (order) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. expected_tds
       (gross_paise * rate_bps + 5000) // 10000   over {'gross_paise': 76289, 'rate_bps': 10}
       = 76 paise
  2. delta
       abs(applied_tds - expected_tds)   over {'applied_tds': 63, 'expected_tds': 76}
       = 13 paise
```

---

### `exc_00109`  ·  D02  ·  ₹0.13 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `int_00105` (intent), `ord_00105` (order), `setl_00097` (recon_row) |
| hypotheses | 1. not investigated -- rules-only configuration |

recompute trace:

```
  1. commission
       (gross_paise * commission_bps + 5000) // 10000   over {'commission_bps': 1600, 'gross_paise': 87134}
       = 13941 paise
  2. expected_transfer
       gross_paise - commission - mdr   over {'commission': 13941, 'gross_paise': 87134, 'mdr': 0}
       = 73193 paise
  3. debit
       min(refund_total + dispute_total, expected_transfer)   over {'dispute_total': 0, 'expected_transfer': 73193, 'refund_total': 0}
       = 0 paise
  4. expected_net
       expected_transfer - debit   over {'debit': 0, 'expected_transfer': 73193}
       = 73193 paise
  5. actual_net
       total_credit_paise - total_debit_paise   over {'total_credit_paise': 73180, 'total_debit_paise': 0}
       = 73180 paise
  6. delta
       expected_net - actual_net   over {'actual_net': 73180, 'expected_net': 73193}
       = 13 paise
```

---

### `exc_00110`  ·  UNMATCHED  ·  ₹0.00 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00136` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00111`  ·  UNMATCHED  ·  ₹0.00 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00136` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00112`  ·  UNMATCHED  ·  ₹0.00 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00137` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

---

### `exc_00113`  ·  UNMATCHED  ·  ₹0.00 at risk

| outcome | `ESCALATED_UNRESOLVED` |
|---|
| what was tried | rules-only configuration |
| what would resolve it | L3 investigation, which is disabled in this ablation arm |
| evidence | `bank_00142` (exception_subject) |
| hypotheses | 1. not investigated -- rules-only configuration |

